# M2 — Wallet

**Status:** Complete  
**Date:** 2026-06-25

## Delivered
- viem wallet with xchacha20poly1305 keystore (`@noble/ciphers` + scrypt key derivation)
- Private key decrypted only at signing time, scrubbed after (`scrubHexKey`)
- Native + ERC-20 balance reads per configured chain (`config/wallet.example.yaml`)
- `sendTransaction` with dry-run default (simulated hash)
- Live send gated by `LIVE_EXECUTION=true` env **and** `dryRun=false` CLI flag
- All RPC calls wrapped in `logExternalCall`

## Acceptance
| Criterion | Result |
|-----------|--------|
| Testnet balances (mocked RPC) | ✅ |
| Dry-run respected | ✅ |
| No private key in logs | ✅ |

## New deps
`viem`, `@noble/ciphers`, `@noble/hashes`

## Next: M3 — LI.FI integration
